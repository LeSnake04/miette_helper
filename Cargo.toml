[package]
name = "miette_helper"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
miette = { version = "5.3" }

[dev-dependencies]
log = "0.4"

[features]
fancy = ["miette/fancy"]